gitlab install
=========================
## https://about.gitlab.com/downloads/

# download rpm
curl -O https://downloads-packages.s3.amazonaws.com/centos-6.6/gitlab-7.6.2_omnibus.5.3.0.ci.1-1.el6.x86_64.rpm
# install
rpm -i gitlab-xxx.rpm
# re-config
vi /etc/gitlab/gitlab.rb
set 'external_url' to real url (host:port)
set "gitlab_rails['gitlab_email_from']" to email from

# reconfigure
gitlab-ctl reconfigure
# other command
gitlab-ctl [start|restart|stop|...]

# login
http://$extenal_url
user/pwd:
    root/5iveL!fe
    or admin@example.com/5iveL!fe

# forget admin password
find info from 'Gitlab database settings' and 'GitLab PostgreSQL' sections
sudo -u gitlab-pstl /opt/gitlab/embedded/bin/postgre 'gitlabhq_production'
select * from users;
# find admin user name to login
# reset email??
gitlab-ctl reconfigure